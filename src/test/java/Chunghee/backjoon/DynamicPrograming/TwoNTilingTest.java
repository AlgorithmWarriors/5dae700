package Chunghee.backjoon.DynamicPrograming;

import org.junit.Test;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.*;
public class TwoNTilingTest {
    /*
    @Test
    public void TwoByN타일링갯수테스트(){
        TwoNTiling twoNTiling= new TwoNTiling();
        assertThat(twoNTiling.solution(2),is(2));
        assertThat(twoNTiling.solution(9),is(55));
    }
    */

}
